
                <div class="control-group">
                <%= link_to  '添加角色 » ', new_role_path(),:class=>"btn" %>
                </div>
      <table class="table table-hover">
        <thead>
          <tr>
            <th>
              角色名称
            </th>
            <th>
              操作
            </th>
          </tr>
        </thead>
        <tbody>
          <% @roles.each do |role| %>
          <tr>
            <td><%= role.name %></td>
            <td>
              <%= link_to '权限', role_path(role) %>
              <%= link_to '查看', role_path(role) %>
              <%= link_to '编辑', edit_role_path(role) %>
              <%= link_to '删除', role, method: :delete, data: { confirm: 'Are you sure?' } %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>