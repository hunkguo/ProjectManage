
                <div class="control-group">
                <%= link_to  '添加用户 » ', new_user_path(),:class=>"btn" %>
                </div>
      <table class="table table-hover">
        <thead>
          <tr>
            <th>
              姓名
            </th>
            <th>
              部门
            </th>
            <th>
              操作
            </th>
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user| %>
          <tr>
            <td><%= user.username %></td>
            <td>
              <ul class="inline">
              <% user.depts.each do |dept| %>
                <li><%= dept.name %></li>
              <% end %>
              </ul>
            </td>
            <td>
              <%= link_to '权限', user_path(user) %>
              <%= link_to '查看', user_path(user) %>
              <%= link_to '编辑', edit_user_path(user) %>
              <%= link_to '删除', user, method: :delete, data: { confirm: 'Are you sure?' } %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>